#ifndef EQUIPMENT_HPP
#define EQUIPMENT_HPP

#include "includes.hpp"

class equipment {
    public:
        /*
            What will I need?
                Card container (infile)
                    Random card picker
                Hand container (array of objects?)
                    Possibly a struct to make a temporary object
                Getters for the selected card (whatever index)
        */
};

#endif